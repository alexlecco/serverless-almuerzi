-------------------------------------------------------------------
# dependencies

$ npm i -S express mongoose body-parser cors
-------------------------------------------------------------------
# commands

// start a project (rename it after)
$ vercel init

// deploy it to vercel.com
$ vercel

// download env variables setup in vercel (cloud)
$ vercel env pull

// run dev server locally
$ vercel dev
-------------------------------------------------------------------